= render 'shared/errors', resource: @question

= form_with model: @question do |f|
  = f.label :title
  = f.text_field :title
  br
  b= f.label :body
  = f.text_area :body
  br
  = f.submit 'Ask'
