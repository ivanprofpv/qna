$('.subscribe-button').addClass('hidden')
$('.subscription-control').append(
   <%= button_to 'Unsubscribe',
                subscription_path(@subscription),
                method: :delete,
                remote: true,
                class: "btn btn-secondary unsubscribe-button" %>
)